import React from 'react';
import Navbar from "../nav/Nav";

const Header = () => {
    return (
        <header className="new-header">
            <Navbar />
        </header>
    );
};

export default Header;
